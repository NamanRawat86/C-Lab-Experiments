#include <stdio.h>

int main() {
    int num = 10;
    // The %p format specifier is used for printing pointers (addresses)
    printf("Value of num: %d\n", num);
    printf("Address of num: %p\n", (void*)&num);
    return 0;
}
